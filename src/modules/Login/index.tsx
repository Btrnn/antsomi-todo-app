// Libraries
import React from 'react';

// Components
import { Button, Input, Checkbox, Form } from 'components/ui';

interface LoginProps {}

export const Login: React.FC<LoginProps> = props => {
  const { ...restProps } = props;

  return <div>Login</div>;
};
